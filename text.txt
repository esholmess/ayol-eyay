Tool / Framework,What You Must Code From Memory

NumPy & Pandas,
    "Array slicing, tensor shapes, handling missing data, data normalization."

Scikit-Learn,
    "Pipeline creation, fit(), predict(), cross-validation, and metrics (F1-score, ROC-AUC)."

PyTorch (Crucial),
    "The explicit training loop, Custom Dataset and DataLoader classes, tensor manipulation."

Hugging Face,
    "Loading pre-trained text/vision encoders (BERT, ResNet) and fine-tuning configurations."

Matplotlib / Seaborn,
    "Quickly plotting loss curves, confusion matrices, and data distributions."

Backpropagation & Gradient Descent variants (SGD vs Adam/AdamW).  

Attention Mechanisms & Transformers 
    (understanding tokenization and why scaling factors are used).

Loss Functions: Knowing exactly when to apply Cross-Entropy 
    (classification) vs MSE/MAE (regression).

Be comfortable utilizing pre-trained backbones. 
Practice downloading a model like BERT or a ResNet variant from Hugging Face / Torchvision, 
freezing the early layers, and adding a custom classification head.
